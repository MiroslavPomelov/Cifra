import React from 'react'

const Form = () => {
  return (
    <div>
        <h1>Введите продукт</h1>
        <input type="text" placeholder='Введите название'/>
        <button>Внести</button>
    </div>
  )
}

export default Form;