import * as Handlebars from 'handlebars';
import * as fileSystem from 'fs';

// const templateData: string = fileSystem.readFileSync('./public/template.hbs', 'utf8');

// // Производим компиляцию шаблона
// const template = Handlebars.compile(templateData);

// const data = {
//     title: 'Greeting with HandkeBars',
//     body: 'Unbeliviable strong and comfartable SSR'
// }

// // Рендеринг
// const result = template(data);

// console.log(result);



