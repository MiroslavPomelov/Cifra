select *
from bookings b 
join rooms r 
on r.room_id = b.room_id 
join  guests g 
on g.guest_id = b.guest_id 
where rtrim(ltrim(room_number, 'R-'), 'A-') ::int  > (select  max(rtrim(ltrim(room_number, 'R-'), 'A-') ::int)
from bookings b 
right join rooms r 
on r.room_id = b.room_id 
where b.booking_id is null);




select  split_part(r.room_number, '-',2)::text
from bookings b 
right join rooms r 
on r.room_id = b.room_id 
where b.booking_id is null;

select  max(rtrim(ltrim(room_number, 'R-'), 'A-') ::int)
from bookings b 
right join rooms r 
on r.room_id = b.room_id 
where b.booking_id is null;